import { SupervisorDeliveryPipe } from './supervisor-delivery.pipe';

describe('SupervisorDeliveryPipe', () => {
  it('create an instance', () => {
    const pipe = new SupervisorDeliveryPipe();
    expect(pipe).toBeTruthy();
  });
});
